package execution.symbols;

/**
 * 记录在符号表中的符号类型
 */
public enum SymValueType {
    INT,
    REAL,
    CHAR,
    INT_ARRAY,
    REAL_ARRAY,
    TRUE,
    FALSE,
    INT_ARRAY_ELEMENT,
    REAL_ARRAY_ELEMENT
}
